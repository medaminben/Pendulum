#ifndef ${lib_NAME_UPPER}_MAINWINDOW_H
#define ${lib_NAME_UPPER}_MAINWINDOW_H

#include <QMainWindow>

QT_BEGIN_NAMESPACE
namespace Ui {
class MainWindow;
}
QT_END_NAMESPACE

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);
    ~MainWindow();
public slots:
void CoreAnswer();
private:
    Ui::MainWindow *ui;
};
#endif // ${lib_NAME_UPPER}_MAINWINDOW_H
